import { FindQuestionOptionsDto } from './find-question-options.dto';

describe('FindQuestionOptionsDto', () => {
  it('should be defined', () => {
    expect(new FindQuestionOptionsDto()).toBeDefined();
  });
});
